@model LoginUser


<h4>CDSS Sign In</h4>

<div class="col-sm-5">
    @if (ViewData["Message"] != null)
    {
        <div class="mb-3">
            <div class="alert alert-secondary">
                <text>@ViewData["Message"]</text>
            </div>
        </div>
    }

    <form asp-controller="Account" asp-action="Login" method="post">
        <div class="mb-3">
            <label asp-for="MedicalStaffId" class="form-label fw-bold">User ID :</label>
            <input asp-for="MedicalStaffId" class="form-control form-control-sm" placeholder="Registered User ID" />
            <span asp-validation-for="MedicalStaffId" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Password" class="form-label fw-bold">Password :</label>
            <input asp-for="Password" class="form-control form-control-sm" placeholder="Password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <input type="submit" class="btn btn-sm btn-primary" value="Sign In" />
        </div>
    </form>

</div>
   
